import client from './client';
import { API_ENDPOINTS } from './endpoints';

/**
 * Bookmarks API functions
 * TODO: Implement when bookmark endpoints are available
 */
export const bookmarksApi = {
  // Placeholder for future bookmark endpoints
  // list: async (): Promise<Bookmark[]> => { ... },
  // create: async (data: CreateBookmarkDto): Promise<Bookmark> => { ... },
  // update: async (id: string, data: UpdateBookmarkDto): Promise<Bookmark> => { ... },
  // delete: async (id: string): Promise<void> => { ... },
};

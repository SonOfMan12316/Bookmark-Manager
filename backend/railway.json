{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -f backend/package.json ]; then npm install --prefix backend && npm run build --prefix backend; else npm install && npm run build; fi"
  },
  "deploy": {
    "startCommand": "if [ -f backend/package.json ]; then npm run start:prod --prefix backend; else npm run start:prod; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}